name: api

# The build section defines how to build the images of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#build
build:

  # You can use the following env vars to refer to this image in your deploy commands:
  #  - OKTETO_BUILD_API_REGISTRY: image registry
  #  - OKTETO_BUILD_API_REPOSITORY: image repo
  #  - OKTETO_BUILD_API_IMAGE: image name
  #  - OKTETO_BUILD_API_SHA: image tag sha256
  api:
    context: .
    dockerfile: Dockerfile

  # You can use the following env vars to refer to this image in your deploy commands:
  #  - OKTETO_BUILD_API_DEV_REGISTRY: image registry
  #  - OKTETO_BUILD_API_DEV_REPOSITORY: image repo
  #  - OKTETO_BUILD_API_DEV_IMAGE: image name
  #  - OKTETO_BUILD_API_DEV_SHA: image tag sha256
  api-dev:
    context: .
    dockerfile: Dockerfile
    target: dev

# The deploy section defines how to deploy your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#deploy
deploy:
  commands:
    - name: Debug
      command: |
        echo "Debugging API deployment..."
        env | grep OKTETO
    - name: Deploy API
      command: helm upgrade --install api ./chart --set image=${OKTETO_BUILD_API_IMAGE} --set load=${API_LOAD_DATA:-true}

# The dependencies section defines other git repositories to be deployed as part of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#dependencies
# dependencies:
#   - https://github.com/okteto/sample


# The dev section defines how to activate a development container
# More info: https://www.okteto.com/docs/reference/manifest/#dev
dev:
  api:
    securityContext:
      runAsUser: 0
      runAsGroup: 0
      fsGroup: 0
      capabilities:
        add:
          - SYS_PTRACE
    image: registry.andreadebug.dev.okteto.net/andrea/movies-api-dev@sha256:deb12e43fd12b9325d810bfd8a0c0d6fd713080d5f773dc1a5073aa392985edf
    mode: sync
    command: bash
    forward:
      - 2346:2345
    sync:
      - api:/usr/src/app
icon: https://apps.okteto.com/movies/icon.png

